# Nixpacks build config for Railway
# Nixpacks detecta pnpm automáticamente desde pnpm-lock.yaml.
# Solo especificamos la versión de Node y el comando de inicio.

[phases.setup]
nixPkgs = ["nodejs_20"]

[start]
cmd = "node server/app.js"
